[package]
name = "kubo-core"
version.workspace = true
edition.workspace = true
description = "Core: Docker-based isolated development environments"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tar = "0.4"
thiserror = "2"
tempfile = "3"
